name: Deploy NY SHIELD Act Readiness to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-ny-shield
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: github/ny-shield/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: github/ny-shield

      - name: Build
        run: npx astro build
        working-directory: github/ny-shield

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: github/ny-shield/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
